/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"], // this array takes files in which tailwind has to be used // soinside src folder , files with {html,js} extension should use tailwind.
  theme: {
    extend: {},
  },
  plugins: [],
}

